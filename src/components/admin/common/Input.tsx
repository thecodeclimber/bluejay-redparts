import { Input } from '@chakra-ui/react';

const InputKey = () => {
  return (
    <Input
      size="sm"
      type="text"
      name="keysearch"
      id="keysearch"
      placeholder="Search"
      width="150px"
    />
  );
};

export default InputKey;
