interface Theme {
  colors: {
    white: string;
    black: string;
    primary: string;
    success: string;
  }
}

const theme: Theme = {
  colors: {
    white: '#FFFFFF',
    black: '#000000',
    primary: '#1E74DF',
    success: '#07BC0C',
  },
};

export default theme;
